import { HomePage } from "@/features/home";

/**
 * `/` route — thin composition root.
 * All UI logic lives in `src/features/home/HomePage`.
 */
export default function Home() {
  return <HomePage />;
}
